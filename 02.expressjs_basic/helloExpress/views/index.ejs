<!-- 이 문서는 html5 표준을 따르는 문서이다 라는 표식, DTD 선언부 (참조부), 생략 가능이지만, 일반적으로는 포함함
      이 문서는 html로 시작해서 html로 끝난다 -> root element를 표현
-->
<!DOCTYPE html>
<html>
  <!--  이문서의 메타 정보 기술  -->
  <head>
    <!--  문서의 제목, 탭에서 노출 -->
    <!-- 
      - element의 구성 : <start tag> contents </end tag>
      - contents는 여러 개의 element, 내용 (데이터)이 올 수 있다
      - <%= title %> : 값을 찍는 ejs의 문법
        - < % = 값 % >
        - 타엔진 < or { or 탭 등등
        - < or { 유형을 더 많이 사용하는 이유 -> html과 구조가 유사
      - ejs 관점에서 html 형식의 ejs 파일은 그냥 텍스트 -> 탐색 -> < % = 값 % > 만나면 처리
     -->
    <title><%= title.v %></title>
    <!-- 외부 CSS 연결 -->
    <!-- 
      - css or js를 html에 연결
        - external : 리소스 원본이 html 외부에 존재 -> 템플릿 엔진에서 제어가 안 됨
        - internal : 리소스 원본이 html 내부에 존재
        - inline   : tag 내부에 기술 (tag의 속성으로 사용할 때)
     -->
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <!--  이 문서의 내용, 화면에 보이는 부분 담당 -->
  <body>
    <h1><%= title.v %></h1>
    <p>Welcome to <%= title.v %></p>
  </body>
</html>
