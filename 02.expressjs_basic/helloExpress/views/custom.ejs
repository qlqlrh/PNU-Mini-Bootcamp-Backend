<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <a href="http://127.0.0.1:3000/gettest?no=1234&nm=부산대">get 방식 전송 테스트</a>
    <!--
        - 사용자로부터 어떤 입력을 받아서 서버로 데이터를 전송할 때 사용
        - action 값은 데이터를 보낼 url 기입, 생략하면 현재 페이지가 됨
        - methond는 전송 방식, default는 get
    -->
    <form>
        <!-- input(text, 날짜, 수치, 이메일, 비번, hidden, ... ),
             select radio, checkbox, textarea -->
        <!-- 
            input 요소
                - 속성 type : 입력 유형
                - 속성 name : 데이터를 지칭, 추출할 때 사용하는 이름/key !!
                - 속성 value : 전달할 값
         -->
         <!-- <xxx/> 단독 tag (input, brm img, ...)-->
        <input type="hidden" name="no" value="5678"/>
        <input type="hidden" name="nm" value="부산"/>
        <input type="submit" value="전송"/>
    </form>
    <br/>
    <div>
      <fieldset>
        <!-- 현재 화면 주소와 데이터를 보낼 주소가 동일하면 action 생략 가능 -->
        <!-- 여기서는 주소가 같지만, 의도적으로 한 번 적어봄 -->
        <!-- required를 넣으면, 이게 입력되지 않으면 form 전송이 안 되게 하는 옵션 -->
        <!-- name은 key 역할을 하기 때문에 무조건 적어줘야함! -->
        <!--
          enctype
            - application/x-www-form-urlencoded : 데이터를 보내기 전에 인코딩 (default 인코딩)
            - multipart/form-data : 문자 인코딩 X, 파일 업로드
            - text/plain : 텍스트 형식으로 전송
            - application/json, application/xml, ... => 프런트단 js에서 전송
            - 기타 바이너리, ...
        -->
        <form action="/posttest", method="post" enctype="application/x-www-form-urlencoded">
          <input type="email"    name="uid" placeholder="아이디 이메일형식", required/>
          <input type="password" name="upw" placeholder="비밀번호 8자리 이상", required/>
          <input type="submit"   value="로그인"/>
        </form>
      </fieldset>
    </div>
  </body>
</html>
